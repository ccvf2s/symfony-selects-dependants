{{ form_start(form) }}
<div class="row">
    <div class="col-lg-4 col-md-4">
        {{ form_row(form.type) }}
    </div>
    <div class="col-lg-4 col-md-4">
        {{ form_row(form.brand) }}
    </div>
    <div class="col-lg-4 col-md-4">
        {{ form_row(form.model) }}
    </div>
</div>
<br><br>
<div class="row">
    <div class="col-lg-4 col-md-4">
        {{ form_row(form.submit) }}
    </div>
</div>
{{ form_end(form) }}